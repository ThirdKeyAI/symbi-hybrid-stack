# Cloud-specific environment
# Copy to cloud/.env and fill in your values.

# Google Cloud project
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1

# GCS bucket for state replication
GCS_STATE_BUCKET=your-project-symbi-state

# Artifact Registry repository name
ARTIFACT_REGISTRY_REPO=symbi
