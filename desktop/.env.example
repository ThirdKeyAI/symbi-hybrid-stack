# Desktop-specific environment (sources root .env)
# Copy to desktop/.env if you need desktop-specific overrides.
#
# Most values come from the root .env file. Only set overrides here.

# Override Symbi ports if needed
# SYMBI_GRPC_PORT=8080
# SYMBI_HTTP_PORT=8081

# Qdrant port
# QDRANT_PORT=6333

# Operations console port
# A2UI_PORT=3001
